package szysz3.planty.ui.theme

import androidx.compose.ui.graphics.Color

// Light Theme Colors
val MutedGreen = Color(0xFF4A7C59)       // Muted green for primary color in dark mode
val AccentGreen = Color(0xFF88C9A1)      // Slightly darker green for accenting the primary color
val SoftGrey = Color(0xFFBCBCBC)        // Warm, calming beige for background
val LightGrey = Color(0xFFE7ECEF)         // Very light gray for surface elements
val DarkGreen = Color(0xFF1D2724)        // Muted dark green for text and icons on light backgrounds
val ErrorRed = Color(0xFFF28B82)         // Soft red for error messages in light mode

// Dark Theme Colors
val AccentTeal = Color(0xFF6C9A8B)       // Muted teal for secondary color in dark mode
val DarkGray = Color(0xFF121212)         // Dark gray for background in dark mode
val DarkBeige = Color(0xFF3E3A39)        // Dark beige for surface elements in dark mode
val LightGray = Color(0xFFDADADA)        // Light gray for text and icons on dark backgrounds
val ErrorRedDark = Color(0xFFCF6679)     // Soft red for error messages in dark mode

// Dark Theme Colors
val SoftTaupe = Color(0xFF5C4E41)       // Darker taupe for harmonious pairing
val DustyRose = Color(0xFF7A4E4E)       // Darker reddish tone for warmth
val OliveGreen = Color(0xFF4C5E43)      // Darker green to bring natural depth
val SlateBlue = Color(0xFF424654)       // Darker blue for contrast
val GoldenSand = Color(0xFF9C875F)      // Darker golden tone for highlights
val WarmGray = Color(0xFF5F564F)        // Darker neutral gray for blending elements
val BurntOrange = Color(0xFFA05B3F)     // Darker orange for vibrancy
val SteelTeal = Color(0xFF345A58)       // Darker teal for a refreshing feel
val MossGreen = Color(0xFF555F38)       // Darker green for natural harmony
val PaleClay = Color(0xFFB59A81)        // Darker clay for muted highlights
val MutedLavender = Color(0xFF5A4D63)   // Darker lavender for a muted touch
val FadedPeach = Color(0xFF9A7A60)      // Darker peach for a warm accent
val DullOlive = Color(0xFF444D3E)       // Darker olive green for depth
val AshBlue = Color(0xFF4D535F)         // Darker blue for calm contrast
val CreamyBeige = Color(0xFFB5A393)     // Darker beige for subtle highlights
val SmokyGray = Color(0xFF4E4742)       // Darker gray with a smoky tint
val PaleBlush = Color(0xFFB59C9A)       // Darker blush for soft warmth
val MistyTeal = Color(0xFF374F4E)       // Darker teal for balance
val WashedSage = Color(0xFF60684F)      // Darker sage green for natural harmony
val CloudClay = Color(0xFFBCA99A)       // Darker clay for subtle highlights

// Light Theme Colors
val PaleSage = Color(0xFFAABDA1)         // Soft muted green for gentle contrast
val CreamyPeach = Color(0xFFF7D9C4)      // Warm pastel peach for highlights
val LavenderMist = Color(0xFFE4D6F4)     // Light lavender for soft accents
val SunlitSand = Color(0xFFF1E3D6)       // Warm sandy beige for highlights
val BlushingRose = Color(0xFFF2CFC9)     // Light rose pink for gentle color
val MorningSky = Color(0xFFCEE5ED)       // Soft powdery blue for cool tones
val DewyMoss = Color(0xFFC6D6B8)         // Light green-gray for earthy accents
val GoldenDusk = Color(0xFFF4E2B4)       // Pale gold for warmth and elegance
val SoftCoral = Color(0xFFF7BFB0)        // Gentle coral for vibrant accents
val SilverHaze = Color(0xFFD4D6D9)       // Neutral silver-gray for balance
val PowderBlue = Color(0xFFAEC4D6)     // Soft blue for a calming accent
val LightCoral = Color(0xFFE6AFAF)     // Gentle coral for warm highlights
val PastelMint = Color(0xFFB6D4B0)     // Muted mint green for natural balance
val PaleLilac = Color(0xFFD4C8E6)      // Soft lilac for a delicate touch
val WarmVanilla = Color(0xFFF1E7D1)    // Creamy vanilla for warm surfaces
val HazyGray = Color(0xFFCECBC8)       // Soft gray for neutral blending
val BlushPeach = Color(0xFFF7D4C6)     // Light peach for subtle warmth
val GentleSage = Color(0xFFC4D4C2)     // Muted sage green for natural harmony
val CloudySky = Color(0xFFCBDCE6)      // Pale sky blue for airy tones
val SoftSand = Color(0xFFE9D8C9)       // Light sandy beige for grounding elements

val DarkCardColors = listOf(
    SoftTaupe,
    DustyRose,
    OliveGreen,
    SlateBlue,
    GoldenSand,
    WarmGray,
    BurntOrange,
    SteelTeal,
    MossGreen,
    PaleClay,
    MutedLavender,
    FadedPeach,
    DullOlive,
    AshBlue,
    CreamyBeige,
    SmokyGray,
    PaleBlush,
    MistyTeal,
    WashedSage,
    CloudClay
)

val LightCardColors = listOf(
    PaleSage,
    CreamyPeach,
    LavenderMist,
    SunlitSand,
    BlushingRose,
    MorningSky,
    DewyMoss,
    GoldenDusk,
    SoftCoral,
    SilverHaze,
    PowderBlue,
    LightCoral,
    PastelMint,
    PaleLilac,
    WarmVanilla,
    HazyGray,
    BlushPeach,
    GentleSage,
    CloudySky,
    SoftSand
)